version = 10

cloudstream {

    description = "Aho (Lang) SDFilm"
    language    = "vi"
    authors = listOf("Aho")

    /**
    * Status int as the following:
    * 0: Down
    * 1: Ok
    * 2: Slow
    * 3: Beta only
    * */
    status = 1 
    
    tvTypes = listOf("SDFilm")
    iconUrl = "icon url"

    isCrossPlatform = true
}
